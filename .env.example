# Backend server (Next.js rewrites proxy API calls here in development)
NEXT_PUBLIC_API_ROOT_URL=http://localhost:3001

# API path used by the frontend client (relative = proxied through Next.js)
NEXT_PUBLIC_API_BASE_URL=/api/v1
